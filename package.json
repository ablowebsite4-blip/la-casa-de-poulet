{
  "name": "casa-de-poulet-menu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p public && cp index.html styles.css script_v4.js manifest.json sw.js menu.json _headers public/ && cp -r icons public/"
  }
}
